# Brute force solution
class Subset
  attr_accessor :nums

  def initialize(nums)
    self.nums = nums
  end

  # Consider elements as a graph, in which all elements are connected
  # Target is to find all possible combinations(paths) from any starting point to any endpoint
  def recursive_solution

  end

  private

  # DFS to get all permutations
  def dfs_helper
  end
end
